name: HeatMind CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  PYTHON_VERSION: "3.14"
  MONGO_URI: mongodb://localhost:27017
  PYTHONPATH: ${{ github.workspace }}

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install linting tools
        run: pip install ruff
      - name: Ruff check
        run: ruff check . --output-format=github
      - name: Ruff format check
        run: ruff format --check .

  test-unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-test.txt
      - name: Run unit tests with coverage
        run: |
          pytest tests/ -v --cov=. --cov-report=xml --cov-report=term-missing --tb=short
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
          flags: unittests
          fail_ci_if_error: false

  test-memory:
    name: Memory Tests (MongoDB)
    runs-on: ubuntu-latest
    services:
      mongo:
        image: mongo:7
        ports:
          - 27017:27017
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-test.txt
      - name: Wait for MongoDB
        run: |
          for i in $(seq 1 30); do
            if mongosh --eval "db.adminCommand('ping')" --quiet 2>/dev/null; then
              echo "MongoDB is ready"
              break
            fi
            echo "Waiting for MongoDB... ($i)"
            sleep 2
          done
      - name: Run memory + monitor tests
        run: |
          pytest tests/test_session.py tests/test_monitor.py -v --tb=short

  test-integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    services:
      mongo:
        image: mongo:7
        ports:
          - 27017:27017
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-test.txt
      - name: Wait for MongoDB
        run: |
          for i in $(seq 1 30); do
            if mongosh --eval "db.adminCommand('ping')" --quiet 2>/dev/null; then
              echo "MongoDB is ready"
              break
            fi
            echo "Waiting for MongoDB... ($i)"
            sleep 2
          done
      - name: Run integration tests
        run: |
          pytest tests/test_integration.py -v --tb=long

  test-edge-cases:
    name: Edge Case Tests
    runs-on: ubuntu-latest
    services:
      mongo:
        image: mongo:7
        ports:
          - 27017:27017
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-test.txt
      - name: Wait for MongoDB
        run: |
          for i in $(seq 1 30); do
            if mongosh --eval "db.adminCommand('ping')" --quiet 2>/dev/null; then
              echo "MongoDB is ready"
              break
            fi
            echo "Waiting for MongoDB... ($i)"
            sleep 2
          done
      - name: Run all edge case tests
        run: |
          pytest tests/ -v -k "edge or Edge or EdgeCases or extreme or negative or empty or long or unicode or missing or nonexistent" --tb=long

  smoke-test:
    name: Smoke Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-test.txt
      - name: Run end-to-end smoke tests
        run: |
          pytest test_all.py -v --tb=short

  build-verify:
    name: Build Verification
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Verify all imports
        run: |
          python -c "from config import *"
          python -c "from api.fortyguard import FortyGuardClient"
          python -c "from agents.router import route_query"
          python -c "from agents.quick_agent import QuickAgent"
          python -c "from agents.deep_agent import DeepAgent"
          python -c "from agents.emergency_agent import EmergencyAgent"
          python -c "from agents.chain_agent import ChainAgent"
          python -c "from agents.nlp_parser import NLParser"
          python -c "from memory.session import SessionMemory"
          python -c "from monitor.loop import MonitorLoop"
          python -c "from utils.alerts import send_alert"
          python -c "from utils.mcp_client import HeatMindMCPClient"
          python -c "from utils.validation import validate_coords, flatten_location_data"
          python -c "from utils.datasets import get_location_context"
          python -c "from monitor.alerts import MonitorAlertManager"
      - name: Verify Streamlit app loads
        run: |
          timeout 5 python -c "import streamlit; print('Streamlit OK')" || true
