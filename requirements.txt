requests>=2.31.0
pymongo>=4.6.0
python-dotenv>=1.0.0
schedule>=1.2.0
rich>=13.7.0
streamlit>=1.45.0
pydeck>=0.9.0

# LLM providers — install at least one for live agentic reasoning
# The app runs with a built-in Mock LLM if none are installed.
openai>=1.30.0
anthropic>=0.40.0
google-generativeai>=0.7.0
